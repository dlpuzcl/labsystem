<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.dlpuzcl.mapper.ApplyMapper">
    <select id="getApplyList" resultType="apply">
        select u.user_name,l.room_name,a.apply_day,a.apply_section,a.apply_week,c.course_name
        from user u,lab_room l,apply a,course c
        WHERE u.user_id=a.user_id and l.room_id=a.room_id and c.course_id = a.course_id
    </select>
</mapper>